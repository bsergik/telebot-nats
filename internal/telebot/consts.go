package telebot

import "time"

const (
	tbPolletTimeout = time.Duration(10 * time.Second)
)

const (
	acceptButton = "✅ accept"
	rejectButton = "❎ reject"
)

const (
	acceptInviteUniqueName = "accept_invite"
	rejectInviteUniqueName = "reject_invite"
)
